# Discord bot token (create in Discord Developer Portal)
DISCORD_TOKEN=your-token-here

# Optional: restrict command sync to a single guild for faster dev
GUILD_ID=

# Optional: channel to post deploy/update notifications
UPDATE_CHANNEL_ID=

# Locale defaults to en; supported: en, de
DEFAULT_LOCALE=en

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Optional: set during deployment to announce version
DEPLOY_VERSION=

# Optional: persistent marker file for last posted deploy version
DEPLOY_MARKER_PATH=.state/last_deploy_version.txt
