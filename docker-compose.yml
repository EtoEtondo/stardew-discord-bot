version: "3.9"

services:
  bot:
    image: ${IMAGE:-ghcr.io/${GHCR_OWNER:-etondo}/stardew-discord-bot:latest}
    build:
      context: .
    env_file:
      - .env
    environment:
      - DEPLOY_VERSION=${DEPLOY_VERSION:-}
    volumes:
      - ./.state:/app/.state
    restart: unless-stopped
